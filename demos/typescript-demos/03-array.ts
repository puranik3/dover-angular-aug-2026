// TS will infer the data based if the variable is assigned an initial value (type inference)
let person = "John";

const names : string[] = [];

names.push( 'Jane Doe' );
names.push( 'Mark Smith' );
// names.push( 123 ); // error

export {}